<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Pink fill color you requested -->
    <solid android:color="#FFA0D7" />

    <!-- Rounded corners -->
    <corners android:radius="28dp" />

    <!-- Provide a small inner padding so text doesn't touch edges -->
    <padding
        android:left="14dp"
        android:top="10dp"
        android:right="14dp"
        android:bottom="10dp" />
</shape>